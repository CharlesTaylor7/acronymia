{
  "scripts": {
    "postinstall": "playwright install --with-deps && npm run tailwind",
    "tailwind": "tailwindcss -i ./input.css -o ./style/output.css"
  },
  "devDependencies": {
    "@playwright/test": "^1.37.1",
    "tailwindcss": "^3.3.3"
  }
}
